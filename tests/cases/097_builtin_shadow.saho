# 参数名与内置函数同名：函数内遮蔽内置（解释器与转译一致，规范 5.3）
# 注：给内置名赋值或重定义是不允许的（会报错），参数遮蔽是唯一的同名场景
函数 重复(文本, 次数)
    结果 = ""
    for i 于 从a到b(0, 次数)
        结果 = 结果 + 文本
    完毕
    返回 结果
完毕
print(重复("ab", 3))
print(长度(文本("x")))
