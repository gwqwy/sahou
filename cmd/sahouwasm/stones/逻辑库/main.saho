# 逻辑库 —— 纯 sahou 实现的逻辑运算与真值表（v2 标准库 stones 包）
# 语言自带的 并且/或者/非 只够写条件，这里补齐课堂里常见的组合运算，
# 还能把任何一个双输入逻辑函数变成标准真值表。

# 异或：一真一假才为真（"只能选一个"）
函数 异或(a, b)
    返回 (a 或者 b) 并且 非 (a 并且 b)
完毕

# 同或：两边一样才为真（异或的反面）
函数 同或(a, b)
    返回 非 异或(a, b)
完毕

# 蕴含：a => b，"只要有 a 就一定有 b"；a 是假时整句总成立
函数 蕴含(a, b)
    返回 (非 a) 或者 b
完毕

# 全真：列表里每个值都为真才算真（空列表也算全真）
函数 全真(值列表)
    for 值 于 值列表
        如果 非 值
            返回 假
        完毕
    完毕
    返回 真
完毕

# 全假：列表里每个值都为假才算真（空列表也算全假）
函数 全假(值列表)
    for 值 于 值列表
        如果 值
            返回 假
        完毕
    完毕
    返回 真
完毕

# 任真：只要有一个值为真就是真（空列表没有真，返回 假）
函数 任真(值列表)
    for 值 于 值列表
        如果 值
            返回 真
        完毕
    完毕
    返回 假
完毕

# 任假：只要有一个值为假就是真（空列表没有假，返回 假）
函数 任假(值列表)
    for 值 于 值列表
        如果 非 值
            返回 真
        完毕
    完毕
    返回 假
完毕

# 真值表：把一个双输入逻辑函数变成标准的四行真值表。
# 返回列表，每行是一个字典 {"甲": …, "乙": …, "结果": …}，
# 顺序是 真真、真假、假真、假假。用法：
#   我的函数 = 函数(a, b) => a 并且 b
#   表 = 真值表(我的函数)
#   print(表[0]["结果"])        # 真
函数 真值表(逻辑函数)
    表 = []
    for 甲 于 [真, 假]
        for 乙 于 [真, 假]
            表.添加({"甲": 甲, "乙": 乙, "结果": 逻辑函数(甲, 乙)})
        完毕
    完毕
    返回 表
完毕
