# 罗马数字 —— 纯 sahou 实现的罗马数字互转（v2 标准库 stones 包）
# 1 到 3999 的数 ↔ 罗马数字文本（标准减法形式），和 中文数字 是一对。
# 只依赖核心内置函数，宿主与浏览器（经 sahou build）都能用。

# 到罗马：数变罗马数字。到罗马(2026) 得 "MMXXVI"；超出 1..3999 报错
函数 到罗马(n)
    如果 n < 1 或者 n > 3999
        抛出("罗马数字只能表示 1 到 3999，收到 " + 文本(n) + "。")
    完毕
    数值表 = [1000, 900, 500, 400, 100, 90, 50, 40, 10, 9, 5, 4, 1]
    记号表 = ["M", "CM", "D", "CD", "C", "XC", "L", "XL", "X", "IX", "V", "IV", "I"]
    结果 = ""
    for i 于 从a到b(0, 13)
        当 n >= 数值表[i]
            结果 = 结果 + 记号表[i]
            n = n - 数值表[i]
        完毕
    完毕
    返回 结果
完毕

# 自罗马：罗马数字变数。自罗马("MMXXVI") 得 2026。
# 按"小值在大值左边就减"的规则累加；写得不够标准（比如 IIII）也算 4，不苛求
函数 自罗马(文)
    数值 = {"I": 1, "V": 5, "X": 10, "L": 50, "C": 100, "D": 500, "M": 1000}
    总数 = 0
    位置 = 0
    末 = 长度(文)
    当 位置 < 末
        字 = 取(文, 位置, 位置 + 1)
        如果 非 包含(数值, 字)
            抛出("不认识罗马记号 \"" + 字 + "\"。")
        完毕
        当前 = 数值[字]
        后面 = 0
        如果 位置 + 1 < 末
            下一个 = 取(文, 位置 + 1, 位置 + 2)
            如果 包含(数值, 下一个)
                后面 = 数值[下一个]
            完毕
        完毕
        如果 当前 < 后面
            总数 = 总数 - 当前
        否则
            总数 = 总数 + 当前
        完毕
        位置 = 位置 + 1
    完毕
    返回 总数
完毕
