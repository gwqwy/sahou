# 数列库 —— 纯 sahou 实现的经典数列（v2 标准库 stones 包）
# 每个数列都"看得见过程"：整串生成出来，方便打印观察规律。
# 只依赖核心内置函数，宿主与浏览器（经 sahou build）都能用。

# 斐波那契数列：前两项是 0 和 1，之后每项等于前两项之和；返回前 项数 项
函数 斐波那契(项数)
    如果 项数 < 0
        抛出("项数不能是负数。")
    完毕
    结果 = []
    前 = 0
    后 = 1
    for i 于 从a到b(0, 项数)
        结果.添加(前)
        暂 = 前 + 后
        前 = 后
        后 = 暂
    完毕
    返回 结果
完毕

# 等差数列：从 首项 开始，每项加 公差，共 项数 项
函数 等差数列(首项, 公差, 项数)
    如果 项数 < 0
        抛出("项数不能是负数。")
    完毕
    结果 = []
    当前 = 首项
    for i 于 从a到b(0, 项数)
        结果.添加(当前)
        当前 = 当前 + 公差
    完毕
    返回 结果
完毕

# 等比数列：从 首项 开始，每项乘 公比，共 项数 项；公比是 0 报错
函数 等比数列(首项, 公比, 项数)
    如果 项数 < 0
        抛出("项数不能是负数。")
    完毕
    如果 公比 == 0
        抛出("等比数列的公比不能是 0。")
    完毕
    结果 = []
    当前 = 首项
    for i 于 从a到b(0, 项数)
        结果.添加(当前)
        当前 = 当前 * 公比
    完毕
    返回 结果
完毕

# 三角形数：第 n 个三角形数 = 1+2+…+n = n×(n+1)/2，像摆三角形的小石子
函数 三角形数(n)
    如果 n < 0
        抛出("项数不能是负数。")
    完毕
    返回 n * (n + 1) / 2
完毕

# 是否完美数：除自身以外的因数加起来恰好等于自己（比如 6 = 1+2+3）
函数 是否完美数(n)
    如果 n < 2
        返回 假
    完毕
    因数和 = 1
    除数 = 2
    当 除数 * 除数 <= n
        如果 n % 除数 == 0
            因数和 = 因数和 + 除数
            如果 除数 != n / 除数
                因数和 = 因数和 + n / 除数
            完毕
        完毕
        除数 = 除数 + 1
    完毕
    返回 因数和 == n
完毕

# 是否水仙花数：三位数，各位数字的立方和恰好等于自己（比如 153 = 1+125+27）
函数 是否水仙花数(n)
    如果 n < 100 或者 n > 999
        返回 假
    完毕
    个位 = n % 10
    十位 = 数学.向下取整(n / 10) % 10
    百位 = 数学.向下取整(n / 100)
    返回 个位 * 个位 * 个位 + 十位 * 十位 * 十位 + 百位 * 百位 * 百位 == n
完毕

# 角谷数列（角谷猜想/冰雹猜想）：偶数减半、奇数三倍加一，任何正整数最后都落到 1
函数 角谷数列(n)
    如果 n < 1 或者 n != 数学.向下取整(n)
        抛出("角谷数列要从正整数出发。")
    完毕
    结果 = [n]
    步数 = 0
    当 n != 1
        如果 n % 2 == 0
            n = n / 2
        否则
            n = 3 * n + 1
        完毕
        结果.添加(n)
        步数 = 步数 + 1
        如果 步数 > 10000
            抛出("一万步还没落到 1，先停下来了。")
        完毕
    完毕
    返回 结果
完毕

# ── 内部辅助 ─────────────────────────
