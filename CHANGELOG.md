# 卅 · 更新说明（CHANGELOG）

> 每个版本一条 `## vX.Y.Z` 小节；Release 工作流发布时自动提取与标签同名的小节作为说明。

## v0.3.0（2026-09-20）

### 新增

- **格子（响应式）转译支持**：`sahou build` 把 `格子` 编译为 JS 响应式运行时
  （惰性求值 + 依赖失效 + 观察者），`页面.绑定/绑输入` 同语义可用——
  转译出的网页与 wasm 直跑行为一致，响应式页面从此可部署到任意静态服务器
- **用户函数默认参数**：`函数 问候(名字, 标点: "！")`；默认值限不可变字面量
  （数/文本/布尔/空值），必须排在参数表末尾，定义处求值一次
- **算法库**新增 汉诺塔（返回每一步）与 八皇后（返回全部 92 个解）——递归与回溯教学
- **LSP 服务端悬停**：内置函数/关键字/标准库模块/stones 成员的签名与说明
  （数据来自 editors/shared/language.json，多编辑器同口径）

### 变更

- stones 十八包清理：数列库/日期库/分数库/时长库 的取整 hack 统一改用 `数学.向下取整`
- `sahou 版本` 横幅改为动态统计（stones 包数不再写死），版本号常量 v0.3.0
- VS Code 扩展 **0.3.0**：悬停转发服务端（stones 成员动态说明），随发版自动附带 vsix

### 修复

- 转译器：与内置函数同名的局部变量/参数与解释器同语义（v0.2.1 回归修复的延续）
- 多行字典字面量的尾逗号与闭括号独立成行（v0.2.1 回归修复的延续）

## v0.2.1（2026-09-20）

### 新增

- stones 标准库扩充到 **十八包**，新增 4 个纯卅库（`用 "包名" 引入` 免安装即用）：
  - **罗马数字**：1–3999 的数 ↔ 罗马数字文本（`到罗马(2026)` 得 `MMXXVI`）
  - **分数库**：精确分数运算——加/减/乘/除/化简/转小数/分数文本/带分数文本，永不丢精度
  - **矩阵库**：矩阵加/乘/转置/单位矩阵/行列式（2×2、3×3）/矩阵文本
  - **时长库**：秒数 ↔ 时分秒——`时长文本(5025)` 得 `1时23分45秒`
- VS Code 扩展 **0.2.1**：十八包的成员补全与悬停文档；`变量名.` 给出列表/字典成员方法

### 变更

- **输出提速**：`打印` 改为缓冲批量写出，循环打印的系统调用减少几个数量级；
  交互场景最迟 50ms 可见；REPL 与 `sahou 打包` 的独立 exe 同享
- `如果/当` 条件里误写单个 `=` 时给出专门双语提示
- LSP 补全：`名字.` 对非模块名给出值成员方法

### 修复

- 多行字典字面量的尾逗号与闭括号独立成行不再报错
- 转译器：与内置函数同名的局部变量/参数与解释器同语义（此前会错误替换成内置函数）
- 给内置函数名赋值或重定义，转译器与解释器一致在编译期拒绝
